/*
 * 기수정렬
 */
var radixSort = (array) => {

    // LSD

    if(isEmptyOROne(array)) {
        return array;
    }

    // 자리수를 판별하기 위함
    let mod = 10;
    let i;
    let digit = 0;

    for(i=0; i < array.lengh; i++) {
        
    }
}